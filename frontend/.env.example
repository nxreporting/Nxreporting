# ============================================================================
# PDF EXTRACTION & REPORTING SYSTEM - ENVIRONMENT VARIABLES
# ============================================================================
# 
# IMPORTANT: Never commit .env.local or .env.production to version control!
# Copy this file to .env.local and fill in your actual values.
#
# ============================================================================

# ----------------------------------------------------------------------------
# DATABASE CONFIGURATION (Supabase PostgreSQL)
# ----------------------------------------------------------------------------
DATABASE_URL="postgresql://user:password@host:port/database"

# ----------------------------------------------------------------------------
# SUPABASE CONFIGURATION
# ----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-here"
SUPABASE_STORAGE_BUCKET="pdf-uploads"

# ----------------------------------------------------------------------------
# JWT AUTHENTICATION
# ----------------------------------------------------------------------------
# Generate a strong secret with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="your-secure-jwt-secret-key-minimum-32-characters-long"
JWT_EXPIRES_IN="7d"

# ----------------------------------------------------------------------------
# OCR API KEYS
# ----------------------------------------------------------------------------

# Nanonets API (Optional - Primary OCR provider if configured)
# Get your API key from: https://app.nanonets.com/
# Note: Requires a paid plan for production use
NANONETS_API_KEY="your-nanonets-api-key-here"
NANONETS_MODEL_ID="your-model-id-here"

# OCR.space API (Recommended - Reliable fallback provider)
# Get your API key from: https://ocr.space/ocrapi
# Free tier: 25,000 requests/month
OCR_SPACE_API_KEY="your-ocr-space-api-key-here"

# OpenAI API (Optional - For advanced AI features)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY="your-openai-api-key-here"

# Hugging Face API (Optional - For AI-powered extraction)
# Get your token from: https://huggingface.co/settings/tokens
HUGGINGFACE_API_TOKEN="your-huggingface-token-here"

# ----------------------------------------------------------------------------
# APPLICATION CONFIGURATION
# ----------------------------------------------------------------------------
NODE_ENV="development"
MAX_FILE_SIZE_MB="50"
NEXT_PUBLIC_FRONTEND_URL="http://localhost:3000"
NEXT_PUBLIC_API_URL="http://localhost:3000/api"

# ----------------------------------------------------------------------------
# SECURITY NOTES
# ----------------------------------------------------------------------------
# 1. Never commit API keys to version control
# 2. Use different keys for development and production
# 3. Rotate keys regularly (every 90 days recommended)
# 4. Enable IP restrictions on API keys when possible
# 5. Monitor API usage for unusual activity
# 6. Use environment-specific .env files:
#    - .env.local (local development - gitignored)
#    - .env.production (production - use Vercel dashboard)
#    - .env.example (template - safe to commit)
#
# ============================================================================
